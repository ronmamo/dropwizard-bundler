server:
  type: default
  applicationConnectors:
    - type: http
      port: 8090
  adminConnectors:
    - type: http
      port: 8091

logging:
  loggers:
    dev.dropwizard.bundler: debug

redis:
  host: localhost
  port: 6379

elastic:
  cluster.name: imdbExample
  path.data: target/data
